on preOpenStack
    if "revmenubar.rev"&quote&",menuPick" is not in the executionContexts then
      close this stack
   else
      if the mode of this stack is not 1 then
         updateUI
      end if
   end if
end preOpenStack

